const NUMBER_OF_CARDS=26,cardNumbers=[...Array(26).keys()].map(x=>x+1);let imageNumbers=[...cardNumbers];const card=document.getElementById("card"),front=document.getElementById("cardFront"),back=document.getElementById("cardBack"),gameEnd=document.getElementById("gameEnd"),startAgain=document.getElementById("startAgain");let frontShown=!0;function startGame(){gameEnd.style.display="none",shuffleArray(imageNumbers),card.classList.remove("flipped"),frontShown=!0,front.style.backgroundImage=`url(assets/${imageNumbers.shift()}.png)`}function shuffleArray(array){for(let i=array.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[array[i],array[j]]=[array[j],array[i]]}}window.onload=()=>{startGame()},startAgain.addEventListener("click",()=>{imageNumbers=[...cardNumbers],startGame()}),card.addEventListener("click",()=>{if(0===imageNumbers.length)return gameEnd.style.display="block",back.style.removeProperty("background-image"),void front.style.removeProperty("background-image");frontShown?back.style.backgroundImage=`url(assets/${imageNumbers.shift()}.png)`:front.style.backgroundImage=`url(assets/${imageNumbers.shift()}.png)`,card.classList.toggle("flipped"),frontShown=!frontShown});